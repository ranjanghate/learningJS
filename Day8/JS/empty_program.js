/*
  Javascript engine creates a global object called window which has function and variable, and window object has global scope when javascript execution context starts
  Chrome uses V8 Engine

  At the global level this === window
*/

console.log(this === window) // At the global level this === window
